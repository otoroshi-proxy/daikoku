include "base.conf"

daikoku {
  mode = "test"
  signingKey = "secret"
  storage = "postgres"

  health {
    accessKey = "secret"
  }

  otoroshi {
    sync {
      cron = false
    }
  }

  postgres {
    database = "daikoku_test"
    poolSize = 4
  }


  tenants {
    provider = "hostname"
  }
}
